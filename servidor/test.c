#include "include/bucket_handler.h"
#include <assert.h>
#include <sys/stat.h>
#include <stdio.h>

int main() {
    //int result = crear_bucket("test");
    //printf("Result: %d\n", result);
    //assert(result == 0);
    //int result = anadir_archivo("test", "servidor/test2.txt");
    //printf("Result: %d\n", result);
    //assert(result == 0);
}
